[workspace]
resolver = "2"
members = [
    "crates/agentlight-core",
    "crates/agentlight-hub-client",
    "crates/agentlight-server",
    "crates/agentlight-source-events",
    "src-tauri",
]

[workspace.package]
version = "0.4.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/ZBVirus/AgentLight"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.9"
sha2 = "0.10"
fs4 = { version = "0.13", features = ["sync"] }
dirs = "6"
notify = "8"
tempfile = "3"
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Fast profile for CI and local test builds. Not used for releases, which keep
# the optimized `release` profile.
[profile.ci]
inherits = "release"
opt-level = 1
lto = false
codegen-units = 16
strip = true
