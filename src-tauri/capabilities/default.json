{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the AgentLight window. Custom commands and all file IO live in Rust, so only core IPC (events + window) is granted here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
